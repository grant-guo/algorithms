package grant.guo.algorithms.list;

import grant.guo.algorithms.list.common.LinkedElement;
import grant.guo.algorithms.list.common.ListUtils;

/**
 * For any integer k, the pivot of a list of integers with respect to k is that list with its
 * nodes reordered so that all nodes containing keys less than k appear before nodes
 * containing k, and all nodes containing keys greater than k appear after the nodes
 * containing k.
 */
public class ListPivoting {
    public static void main(String[] args) {
        LinkedElement equals = null;
        LinkedElement greater = null;
        LinkedElement smaller = null;

        LinkedElement list = ListUtils.generateList();
        int target = 4;

        while(list != null) {

        }
    }
}
